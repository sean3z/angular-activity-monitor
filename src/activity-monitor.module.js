'use strict';

(function() {
    /**
     * @ngdoc           module
     * @name            ActivityMonitor
     * @description     Module containing the activityMonitor service.
     *  Usage:
     *  1. angular.module('myModule', ['ActivityMonitor']);
     *  2. MyController.$inject = ['ActivityMonitor'];
     *  3. function MyController(ActivityMonitor) {
     */
    angular.module('ActivityMonitor', []);
})();
